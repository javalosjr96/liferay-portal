@component-name = "portal-db-infrastructure"
definition {

	property ci.retries.disabled = "true";
	property custom.properties = "feature.flag.LPD-11342=true";
	property database.partition.enabled = "true";
	property database.types = "mysql";
	property test.liferay.virtual.instance = "false";
	property testray.main.component.name = "Database Partitioning";

	test CanNavigateToSitePortletAfterVisitingControlPanel {
		SignIn.signInTestSetup();

		task ("Go to Sites admin so the product menu toggle is visible") {
			ApplicationsMenu.gotoPortlet(
				category = "Sites",
				panel = "Control Panel",
				portlet = "Sites");
		}

		task ("Navigate to Site Builder via the product menu while the Control Panel is open") {
			ProductMenu.gotoPortlet(
				category = "Site Builder",
				portlet = "Pages",
				site = "Guest");
		}

		task ("Navigate to Content and Data via the product menu from the site context") {
			Navigator.openSiteURL(siteName = "Guest");

			ProductMenu.gotoPortlet(
				category = "Content & Data",
				portlet = "Web Content");

			SiteAdmin.viewPortletTitle(portletTitle = "Web Content");
		}
	}

}